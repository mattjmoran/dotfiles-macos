- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: .gitconfig
    ~/.vimrc: .vimrc
    ~/.config/fish: 
      glob: true
      force: true
      create: true
      path: .config/fish

- shell:
    - command: sh brew.sh
      stdout: true
      stderr: true  
